version: "3"

includes:
  scaling-queue-postgres:
    taskfile: ./scaling-queue-postgres
    dir: .

tasks:
  build:
    desc: Build every Bazel target.
    cmds:
      - bazel build //...

  test:
    desc: Run every unit test.
    cmds:
      - bazel test //...

  check:
    desc: Build and check formatting. Run before declaring work done.
    cmds:
      - bazel build //...
      - bazel run //:format.check
